# Progressive docstring/type hardening baseline (task 1.1):
# - Keep current active rules unchanged for now.
# - Enable new enforcement incrementally per OpenSpec rollout slices.
# - Scope expansion must happen via dedicated slice commits.
line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "I", "UP"]
# Task 1.2 initial ratchet: enforce a minimal docstring/type baseline for the
# first target package group (`application`) while keeping other package groups
# relaxed until their dedicated remediation slices.
# Task 2.1 ratchet step: expand application enforcement to public class/method/
# function docstrings while keeping type signature checks active.
extend-select = ["D100", "D101", "D102", "D103", "ANN001", "ANN201"]

[lint.per-file-ignores]
# Non-targeted groups for task 1.2 (explicitly deferred to later slices).
"src/triage_automation/config/**/*.py" = ["D100", "D101", "D102", "D103", "ANN001", "ANN201"]
"apps/**/*.py" = ["D100", "D101", "D102", "D103", "ANN001", "ANN201"]
"tests/**/*.py" = ["D100", "D101", "D102", "D103", "ANN001", "ANN201"]
"alembic/**/*.py" = ["D100", "D101", "D102", "D103", "ANN001", "ANN201"]
